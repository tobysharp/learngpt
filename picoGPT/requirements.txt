numpy # used for the actual model code/weights
regex # used by the bpe tokenizer
requests # used to download gpt-2 files from openai
tqdm # progress bar to keep your sanity
fire # easy CLI creation

# used to load the gpt-2 weights from the open-ai tf checkpoint
# M1 Macbooks require tensorflow-macos
tensorflow; sys_platform != 'darwin' or platform_machine != 'arm64'
tensorflow-macos; sys_platform == 'darwin' and platform_machine == 'arm64'
